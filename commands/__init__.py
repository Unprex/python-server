# -*- coding: utf-8 -*-

""" Commands module

*client* and *server* contains setups for client and server states.

*command* contains handlers for commands in different states.

*status* contains handlers for packets in the status state.

"""
